services:
  mailrelay:
    build: .
    image: mailrelay:latest
    container_name: mailrelay
    restart: unless-stopped
    env_file: .env
    ports:
      # 局域网 HTTP 直连（默认）。只想让同机反代访问时改成 "127.0.0.1:${HTTP_PORT:-9005}:9005"
      - "9005:8080"
    volumes:
      - ./data:/app/data
